import {styled, Button} from '@mui/material';
import React from 'react'

export default function CustomButtonComponent({sx}) {
  return (
    <CustomButton sx={sx}>CustomButtonComponent</CustomButton>
  )
}
